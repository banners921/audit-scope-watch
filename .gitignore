# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets & local-only (never commit)
.env
.env.*
MIGRATION_PACKAGE/
STATUS_AUTO.md
.claude/
scripts/

# Local-only technical briefing — contains security notes, never publish (repo is public)
AUDITSCOPE_BRIEFING.md

# Supabase CLI local state (contains project ref + pooler URL) — never commit
supabase/.temp/
